{% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
{% endblock %}
<header>
    <nav class="navbar navbar-inverse navbar-expand-lg navbar-light bg-navbar">
        <a class="navbar-brand text-option" href="{{ path('app_index') }}">WILD SERIES</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-item nav-link active text-home" href="{{ path('app_index') }}">Home <span class="sr-only">(current)</span></a>
                <a class="nav-item nav-link text-option" href="{{ path('program_index') }}">Programs</a>
                <a class="nav-item nav-link text-option" href="#">Pricing</a>
            </div>
        </div>

        <div>
            {% if app.user %}
                {{ app.user.email }}
                <a href="{{ path('app_logout') }}"> Se déconnecter</a>
            {% else %}
                <a href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}
        </div>
    </nav>


</header>



